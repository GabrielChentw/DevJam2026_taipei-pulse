# 複製成 .env 後填入實際值，或直接在啟動前用 $env:GEMINI_API_KEY="..." 設定。
# .env 不會被自動載入——目前沒有接 python-dotenv，見 README 的啟動指令。

# Gemini API 金鑰。到 https://aistudio.google.com/app/apikey 取得。
# 未設定時 /api/chat 會回 503，錯誤訊息會附上取得金鑰的連結。
GEMINI_API_KEY=

# 可選。預設 gemini-2.5-flash。
TAIPEI_PULSE_MODEL=gemini-2.5-flash
